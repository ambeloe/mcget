designed to be easy to stuff into bash scripts

Usage:
    latest version:
        ./mcget lver {channel}
            channel is either "release" or "snapshot"

    version server url:
        ./mcget url {version}

    version server sha1 sum:
        ./mcget sha1 {version}

    version java version:
        ./mcget jver {version}
